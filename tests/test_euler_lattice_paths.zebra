int w = 20;
int h = 20;

func int fact(int x){
    if (x == 1 || x == 0){
        return 1;
    }
    else{
        return fact(x-1) * x;
    }
}

int n = fact(w + h);

int d = fact(w) * fact(h);
zout(n/d);