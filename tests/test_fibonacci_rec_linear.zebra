int temp = 0;
func int fib(int num){
    if (num == 2){
        return 1;
    }
    else{
        int x = fib(num-1);
        int new = x + temp;
        temp = x;
        return new;
    }
}

int z = fib(142);

zout(z);