var int temp = 0;
func int fib(int num){
    if (num == 2){
        return 1;
    }
    else{
        var int x = fib(num-1);
        var int new = x + temp;
        temp = x;
        return new;
    }
}

var int z = fib(142);

zout(z);